# Dataset (4.26 GB — downloaded via download_dataset.sh)
/data/

# MediaPipe Face Landmarker model (downloaded via download_dataset.sh)
face_landmarker.task

# Precomputed artifacts (large .npy files)
precomputed/

# Utama replication artifacts
/precomputed_utama/
/precomputed_aripin/

# Python cache
__pycache__/
*.py[cod]

# macOS metadata
.DS_Store

# Runtime logs and generated figures (model checkpoints remain tracked)
/output/logs/*
!/output/logs/.gitkeep
/output/figures/*
!/output/figures/.gitkeep
# GPU PC access skill (contains credentials)
skills/gpu-pc.md

# Agent context file (local-only)
AGENTS.md

# Python venv
.venv/
